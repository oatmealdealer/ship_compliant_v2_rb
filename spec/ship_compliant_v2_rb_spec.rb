# frozen_string_literal: true

RSpec.describe ShipCompliantV2 do
  it "has a version number" do
    expect(ShipCompliantV2::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
